# Set default behavior to LF for all text files
* text=auto eol=lf

# Set default behavior to LF for all shell scripts
*.sh text eol=lf

# Set default behavior to CRLF for all Windows batch files
*.bat text eol=crlf
